using System;
using System.Collections.Generic;
using System.Text;
using System.Linq;

namespace HackRank
{
    class E051_PrimeReplace
    {
        //static int nmbrLngth = 2;
        static string nmbrPtrn = "*3";

        public static void runProc()
        {
            //for (int idx = 1; idx < limit; idx = idx+2)
            //{
            //    if (IsPrime.CheckForPrime(idx))
            //    {
            //        primeArray.Add(idx);
            //        sumPrimes += idx; 
            //    }
            //}
            //Console.WriteLine("sumPrimes:" + primeArray.Sum()); ;
            //Console.WriteLine("sumPrimes:" + sumPrimes);
        }
   }
}
